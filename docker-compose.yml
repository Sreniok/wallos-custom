services:
  wallos:
    container_name: wallos
    image: wallos-custom:latest
    build:
      context: ./app
      dockerfile: Dockerfile
    ports:
      - "8282:80/tcp"
    environment:
      TZ: 'Europe/Dublin'
    # Volumes store your data between container upgrades
    volumes:
      - './db:/var/www/html/db'
      - './logos:/var/www/html/images/uploads/logos'
    restart: unless-stopped
